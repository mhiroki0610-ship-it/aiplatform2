"use client";import {useSearchParams} from "next/navigation";import {Download} from "lucide-react";export function CsvLink(){const ip=useSearchParams().get("ip")||"kimetsu-no-yaiba";return <a className="no-print fixed bottom-20 right-4 z-30 flex min-h-11 items-center gap-2 rounded-full border border-slate-200 bg-white px-4 text-xs font-bold text-brand-700 shadow-lg lg:bottom-5" href={`/api/export-csv?ip=${ip}`}><Download size={16}/>CSV出力</a>}
