import {NextResponse} from "next/server";import {getDataset} from "@/lib/ip-datasets";import Papa from "papaparse";
export async function GET(req:Request){const url=new URL(req.url),data=getDataset(url.searchParams.get("ip"));const csv=Papa.unparse(data.promptResults.map(x=>({...x,competitorsMentioned:x.competitorsMentioned.join("|"),sourceReferences:x.sourceReferences.join("|")})));return new NextResponse("\ufeff"+csv,{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":`attachment; filename="${data.slug}-ai-search.csv"`}})}
