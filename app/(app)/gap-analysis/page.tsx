import {ArrowRight,Download} from "lucide-react";import Link from "next/link";import {PageHeader,PriorityBadge} from "@/components/dashboard/ui";import {gapInsights} from "@/lib/data";
export default function Page(){return <><PageHeader eyebrow="AI × SNS Opportunity" title="Gap Analysis" description="AI上の見え方とSNS熱量の差を、競合・情報品質・多言語・収益化の観点で機会へ変換します。" actions={<a className="btn btn-outline" href="/api/export-csv?type=gaps"><Download size={16}/>CSV Export</a>}/><div className="mb-6 flex flex-wrap gap-2">{["すべて","AI弱・SNS強","競合劣後","誤情報リスク","公式情報不足","多言語不足","収益化機会"].map((x,i)=><button key={x} className={`rounded-full px-4 py-2 text-xs font-bold ${i===0?"bg-ink text-white":"border border-slate-200 bg-white text-slate-600"}`}>{x}</button>)}</div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{gapInsights.map(g=><article className="card flex flex-col p-5" key={g.id}><div className="flex items-center justify-between"><span className="badge bg-blue-50 text-blue-700">{g.gapType}</span><PriorityBadge value={g.severity}/></div><h2 className="mt-4 text-lg font-bold">{g.title}</h2><p className="mt-2 flex-1 text-sm leading-6 text-slate-500">{g.description}</p><div className="mt-5 rounded-xl bg-slate-50 p-4"><div className="flex justify-between text-xs text-slate-500"><span>Opportunity score</span><b className="text-xl text-ink">{g.opportunityScore}</b></div><div className="mt-2 h-1.5 rounded-full bg-slate-200"><div className="h-full rounded-full bg-brand-500" style={{width:`${g.opportunityScore}%`}}/></div></div><div className="mt-4 text-xs"><span className="text-slate-400">関連文脈</span><b className="ml-2">{g.relatedContext}</b></div><div className="mt-3 text-xs"><span className="text-slate-400">想定インパクト</span><b className="ml-2 text-emerald-600">{g.expectedImpact}</b></div><Link href={`/recommendations#${g.recommendedActionId}`} className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-sm font-bold text-brand-600">推奨施策を見る <ArrowRight size={16}/></Link></article>)}</div></>}
